# dsh bundle patch: inserts this plugin into a profile's layer stack.
# `id` is this row's key in the layer stack — short and unscoped. `name` is the
# PACKAGE to resolve, so it carries the scope.
- insert:
    - id: dsh-plugin-market
      name: '@99galaxy/dsh-plugin-market'
